import React from 'react';

const Chat = () => {
  return (
    <div style={styles.container}>
      <h2>Chat Page</h2>
      <p>This is the chat page. You can add your chat functionality here.</p>
    </div>
  );
};

// Optional styles for the blank template
const styles = {
  container: {
    padding: '20px',
    textAlign: 'center',
  },
};

export default Chat;
